# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'ruby-kafka'

# gRPC は使わないがコードの自動生成に使う
gem 'grpc-tools'

gem 'google-protobuf'
